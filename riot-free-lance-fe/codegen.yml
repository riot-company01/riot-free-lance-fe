overwrite: true

schema:
  - "https://famous-walrus-45.hasura.app/v1/graphql"

documents: "src/schemas/**/*.graphql"
generates:
  ./src/lib/graphql/:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-react-apollo"
    preset: "client"
