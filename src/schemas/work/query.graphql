query GetWork @cached {
  work {
    contractType
    id
    location
    maxMonthlyPrice
    maxWorkHours
    minMonthlyPrice
    minWorkHours
    title
    languages {
      skill {
        id
        name
        type
      }
    }
  }
}
