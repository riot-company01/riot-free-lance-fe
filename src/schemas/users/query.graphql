query GetUser($id: String) @cached(ttl: 0) {
  users(where: { id: { _eq: $id } }) {
    userName
    userNameKana
    mail
    tel
  }
}

query GetUserToWorks($id: String) {
  users(where: { id: { _eq: $id } }) {
    user_to_works {
      work_id
      favorite
      application
    }
  }
}
