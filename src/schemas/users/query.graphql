query GetUser($id: String) @cached(ttl: 600) {
  users(where: { id: { _eq: $id } }) {
    id
    name
  }
}

query GetFavariteWorks($id: String) {
  user_to_works(where: { user_id: { _eq: $id } }) {
    work_id
  }
}
